package com.MagePVP.Handlers;

import com.MagePVP.Utils.GameState;

public class GameManager implements Runnable {
	
	public void run() {
		
		if (GameState.isCurrentState(GameState.IN_LOBBY)) {
			
			
		}
		
		if (GameState.isCurrentState(GameState.IN_GAME)) {
			
			
		}
		
		if (GameState.isCurrentState(GameState.POST_GAME)) {
			
			
		}
		
		if (GameState.isCurrentState(GameState.RESETTING)) {
			
			
		}
	}

}
